{
  "name": "setm8l-push-server",
  "version": "1.0.0",
  "description": "Push notification server for M8L Set Card Game",
  "main": "send-push.js",
  "scripts": {
    "generate-vapid-keys": "npx web-push generate-vapid-keys --json",
    "send": "node send-push.js"
  },
  "dependencies": {
    "web-push": "^3.6.7",
    "firebase-admin": "^12.0.0"
  }
}
